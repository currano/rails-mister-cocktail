  <div><%= @cocktail.name %></div>
  <% @cocktail.doses.each do |d| %>
    <div><%= d.description %> <%= d.ingredient.name %></div>

  <% end  %>
  <br></br>
  <div><%= link_to "add dose", new_cocktail_dose_path(@cocktail) %></div>
  <%= link_to "back to all cocktails", cocktails_path %>
